# 管理后台功能测试指南

## 🧪 测试环境准备

### 1. 数据库准备
```bash
# 确保 user_quotas 表已创建
mysql -u root -p biomni < agent/create_quota_table.sql
```

### 2. 启动服务
```bash
# 启动 Spring Boot 后端
cd admin/backend
mvn spring-boot:run

# 启动前端
cd admin/frontend
npm run dev
```

### 3. 登录管理后台
- 访问：http://localhost:5173
- 使用管理员账号登录

---

## 📋 功能测试清单

### 一、配额管理功能测试

#### 1.1 查看用户配额
**测试步骤**：
1. 登录管理后台
2. 进入"系统管理" → "用户管理"
3. 查看用户列表中的"Token配额"列

**预期结果**：
- ✅ 显示格式：`XXK / XXXK`（已使用/总配额）
- ✅ 显示使用率百分比
- ✅ 使用率颜色编码：
  - 绿色：< 50%
  - 黄色：50-80%
  - 红色：> 80%

#### 1.2 打开配额管理弹窗
**测试步骤**：
1. 在用户列表中点击某个用户的"配额"按钮

**预期结果**：
- ✅ 弹窗打开
- ✅ 显示用户名
- ✅ 显示总配额（可编辑）
- ✅ 显示已使用量（只读）
- ✅ 显示剩余量（只读）
- ✅ 显示使用率进度条（带颜色）

#### 1.3 更新用户配额
**测试步骤**：
1. 打开配额管理弹窗
2. 修改"总配额 (Tokens)"为 `2000000`
3. 点击"保存"

**预期结果**：
- ✅ 显示"配额更新成功"提示
- ✅ 弹窗关闭
- ✅ 用户列表刷新，显示新的配额

**验证**：
```sql
SELECT * FROM user_quotas WHERE user_id = 1;
-- 检查 total_token_limit 是否为 2000000
```

#### 1.4 重置配额使用量
**测试步骤**：
1. 打开配额管理弹窗
2. 点击"已使用 (Tokens)"行的"重置"按钮
3. 确认操作

**预期结果**：
- ✅ 显示"配额已重置"提示
- ✅ 已使用量变为 0
- ✅ 使用率变为 0%
- ✅ 进度条清空

**验证**：
```sql
SELECT * FROM user_quotas WHERE user_id = 1;
-- 检查 total_token_used 是否为 0
```

#### 1.5 新用户自动创建配额
**测试步骤**：
1. 创建一个新用户
2. 查看该用户的配额信息

**预期结果**：
- ✅ 自动显示默认配额：1,000,000 Token
- ✅ 已使用量为 0
- ✅ 使用率为 0%

**验证**：
```sql
SELECT * FROM user_quotas WHERE user_id = (新用户ID);
-- 检查是否自动创建了配额记录
```

---

### 二、对话记录管理功能测试

#### 2.1 查看对话列表
**测试步骤**：
1. 登录管理后台
2. 进入"系统管理" → "对话记录"

**预期结果**：
- ✅ 显示所有用户的对话列表
- ✅ 显示对话ID、用户信息、标题、状态、消息数、Token数、时间
- ✅ 显示总记录数
- ✅ 分页功能正常

#### 2.2 关键词搜索
**测试步骤**：
1. 在搜索框输入对话标题关键词（如"基因"）
2. 点击"搜索"

**预期结果**：
- ✅ 只显示标题包含关键词的对话
- ✅ 更新总记录数
- ✅ 分页重置为第1页

#### 2.3 状态筛选
**测试步骤**：
1. 在状态下拉框选择"活跃"
2. 点击"搜索"

**预期结果**：
- ✅ 只显示状态为"活跃"的对话
- ✅ 更新总记录数

#### 2.4 日期范围筛选
**测试步骤**：
1. 选择日期范围（如：2025-01-01 到 2025-01-20）
2. 点击"搜索"

**预期结果**：
- ✅ 只显示该日期范围内创建的对话
- ✅ 更新总记录数

#### 2.5 组合筛选
**测试步骤**：
1. 输入关键词
2. 选择状态
3. 选择日期范围
4. 点击"搜索"

**预期结果**：
- ✅ 同时应用所有筛选条件
- ✅ 显示符合所有条件的对话

#### 2.6 重置筛选
**测试步骤**：
1. 设置多个筛选条件
2. 点击"重置"按钮

**预期结果**：
- ✅ 清空所有筛选条件
- ✅ 显示所有对话
- ✅ 分页重置为第1页

#### 2.7 删除对话
**测试步骤**：
1. 找到一个测试对话
2. 点击"删除"按钮
3. 确认删除

**预期结果**：
- ✅ 显示"删除成功"提示
- ✅ 对话从列表中消失
- ✅ 总记录数减1

**验证**：
```sql
SELECT * FROM conversations WHERE id = (对话ID);
-- 检查 deleted_at 字段是否有值（软删除）
```

#### 2.8 分页功能
**测试步骤**：
1. 如果总记录数 > 20，点击分页器的下一页

**预期结果**：
- ✅ 显示下一页的对话
- ✅ 分页器高亮当前页
- ✅ 保持筛选条件

---

## 🔍 边界情况测试

### 配额管理

#### 1. 配额为0
**测试**：设置用户配额为 0
**预期**：
- ✅ 可以保存
- ✅ 使用率显示为 0%（避免除以0错误）

#### 2. 已使用量超过配额
**测试**：手动修改数据库，使 `total_token_used > total_token_limit`
**预期**：
- ✅ 使用率显示 > 100%
- ✅ 进度条显示为红色
- ✅ 不会报错

#### 3. 负数配额
**测试**：尝试输入负数配额
**预期**：
- ✅ 前端验证阻止（min=0）
- ✅ 后端验证阻止（@Min(0)）

### 对话管理

#### 1. 空列表
**测试**：筛选条件导致没有结果
**预期**：
- ✅ 显示"暂无对话记录"
- ✅ 不报错

#### 2. 无效日期范围
**测试**：结束日期早于开始日期
**预期**：
- ✅ 日期选择器自动调整
- ✅ 或显示提示信息

#### 3. 删除不存在的对话
**测试**：尝试删除已删除的对话
**预期**：
- ✅ 显示"对话不存在"错误
- ✅ 不会崩溃

---

## 🚀 性能测试

### 1. 大数据量测试

#### 配额管理
**测试**：
- 创建 100 个用户
- 查看用户列表加载时间

**预期**：
- ✅ 加载时间 < 2秒
- ✅ 配额信息正确显示

#### 对话管理
**测试**：
- 创建 1000 条对话记录
- 查看对话列表加载时间

**预期**：
- ✅ 加载时间 < 2秒
- ✅ 分页正常工作

### 2. 并发测试

**测试**：
- 多个管理员同时更新不同用户的配额
- 多个管理员同时查看对话列表

**预期**：
- ✅ 不会出现数据冲突
- ✅ 所有操作都能成功

---

## 📊 数据验证

### 配额数据一致性

```sql
-- 检查配额表数据
SELECT 
    u.id,
    u.email,
    q.total_token_limit,
    q.total_token_used,
    (q.total_token_limit - q.total_token_used) as remaining,
    ROUND((q.total_token_used / q.total_token_limit) * 100, 2) as usage_percent
FROM admin u
LEFT JOIN user_quotas q ON u.id = q.user_id
WHERE u.deleted_at IS NULL;
```

### 对话数据一致性

```sql
-- 检查对话统计
SELECT 
    c.id,
    c.user_id,
    c.title,
    c.message_count,
    c.total_tokens,
    COUNT(m.id) as actual_message_count,
    SUM(m.tokens) as actual_total_tokens
FROM conversations c
LEFT JOIN messages m ON c.id = m.conversation_id
WHERE c.deleted_at IS NULL
GROUP BY c.id
HAVING c.message_count != actual_message_count 
    OR c.total_tokens != actual_total_tokens;
-- 应该返回空结果（数据一致）
```

---

## ✅ 测试检查清单

### 配额管理
- [ ] 查看用户配额列表
- [ ] 打开配额管理弹窗
- [ ] 更新用户配额
- [ ] 重置配额使用量
- [ ] 新用户自动创建配额
- [ ] 配额为0的边界情况
- [ ] 已使用量超过配额的情况
- [ ] 负数配额验证

### 对话管理
- [ ] 查看对话列表
- [ ] 关键词搜索
- [ ] 状态筛选
- [ ] 日期范围筛选
- [ ] 组合筛选
- [ ] 重置筛选
- [ ] 删除对话
- [ ] 分页功能
- [ ] 空列表显示
- [ ] 删除不存在的对话

### 性能测试
- [ ] 大数据量加载（100+ 用户）
- [ ] 大数据量加载（1000+ 对话）
- [ ] 并发操作测试

### 数据验证
- [ ] 配额数据一致性
- [ ] 对话统计一致性

---

## 🐛 常见问题排查

### 1. 配额不显示
**可能原因**：
- user_quotas 表未创建
- 用户没有配额记录

**解决方法**：
```sql
-- 检查表是否存在
SHOW TABLES LIKE 'user_quotas';

-- 为用户创建配额
INSERT INTO user_quotas (user_id, total_token_limit, total_token_used)
VALUES (1, 1000000, 0);
```

### 2. 对话列表为空
**可能原因**：
- conversations 表为空
- 所有对话都被软删除

**解决方法**：
```sql
-- 检查对话数据
SELECT COUNT(*) FROM conversations WHERE deleted_at IS NULL;

-- 恢复软删除的对话（测试用）
UPDATE conversations SET deleted_at = NULL WHERE id = 1;
```

### 3. 权限错误
**可能原因**：
- 使用非管理员账号登录

**解决方法**：
```sql
-- 检查用户角色
SELECT id, email, role FROM admin WHERE email = 'your@email.com';

-- 设置为管理员
UPDATE admin SET role = 'admin' WHERE id = 1;
```

---

## 📝 测试报告模板

```markdown
# 测试报告

**测试日期**: 2025-01-20
**测试人员**: [姓名]
**测试环境**: 
- 后端：Spring Boot 3.x
- 前端：Vue 3 + Naive UI
- 数据库：MySQL 8.0

## 测试结果

### 配额管理
- [x] 查看配额：通过
- [x] 更新配额：通过
- [x] 重置配额：通过
- [ ] 边界测试：失败（原因：...）

### 对话管理
- [x] 查看列表：通过
- [x] 筛选功能：通过
- [x] 删除对话：通过
- [x] 分页功能：通过

## 发现的问题
1. [问题描述]
   - 严重程度：高/中/低
   - 复现步骤：...
   - 预期结果：...
   - 实际结果：...

## 建议
1. [改进建议]
```

---

**文档版本**: v1.0  
**创建时间**: 2025-01-20  
**维护者**: Biomni Team
