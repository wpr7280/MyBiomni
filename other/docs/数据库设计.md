# Biomni Agent 应用数据库设计

## 1. 数据库概述

### 1.1 数据库信息
- **数据库类型**: MySQL 8.0+
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **存储引擎**: InnoDB
- **时区**: UTC

### 1.2 命名规范
- 表名: 小写字母 + 下划线，复数形式 (如 `users`, `conversations`)
- 字段名: 小写字母 + 下划线 (如 `user_id`, `created_at`)
- 索引名: `idx_表名_字段名` (如 `idx_users_email`)
- 唯一索引: `uk_表名_字段名` (如 `uk_users_username`)
- 外键: `fk_表名_关联表名` (如 `fk_conversations_users`)

### 1.3 通用字段
所有表都包含以下字段:
- `id`: BIGINT, 主键, 自增
- `created_at`: DATETIME, 创建时间, 默认 CURRENT_TIMESTAMP
- `updated_at`: DATETIME, 更新时间, 默认 CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- `deleted_at`: DATETIME, 软删除时间, 默认 NULL (软删除标记)

---

## 2. 数据表设计

### 2.1 用户管理模块

#### 2.1.1 管理员表 (admins)
**表说明**: 存储系统管理员信息

```sql
CREATE TABLE `admins` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '管理员ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '密码(BCrypt加密)',
  `email` VARCHAR(100) NOT NULL COMMENT '邮箱',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `real_name` VARCHAR(50) DEFAULT NULL COMMENT '真实姓名',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用, 1-启用',
  `role` VARCHAR(20) NOT NULL DEFAULT 'admin' COMMENT '角色: super_admin, admin',
  `last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间',
  `last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP',
  `login_fail_count` INT NOT NULL DEFAULT 0 COMMENT '登录失败次数',
  `locked_until` DATETIME DEFAULT NULL COMMENT '锁定截止时间',
  `force_password_change` TINYINT NOT NULL DEFAULT 0 COMMENT '强制修改密码: 0-否, 1-是',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_admins_username` (`username`),
  UNIQUE KEY `uk_admins_email` (`email`),
  KEY `idx_admins_status` (`status`),
  KEY `idx_admins_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员表';
```

#### 2.1.2 用户表 (users)
**表说明**: 存储普通用户信息

```sql
CREATE TABLE `users` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '密码(BCrypt加密)',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `real_name` VARCHAR(50) DEFAULT NULL COMMENT '真实姓名',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用, 1-启用',
  `email_verified` TINYINT NOT NULL DEFAULT 0 COMMENT '邮箱验证: 0-未验证, 1-已验证',
  `phone_verified` TINYINT NOT NULL DEFAULT 0 COMMENT '手机验证: 0-未验证, 1-已验证',
  `register_source` VARCHAR(20) DEFAULT 'email' COMMENT '注册来源: email, phone, google, github',
  `last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间',
  `last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP',
  `login_fail_count` INT NOT NULL DEFAULT 0 COMMENT '登录失败次数',
  `locked_until` DATETIME DEFAULT NULL COMMENT '锁定截止时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_users_username` (`username`),
  UNIQUE KEY `uk_users_email` (`email`),
  UNIQUE KEY `uk_users_phone` (`phone`),
  KEY `idx_users_status` (`status`),
  KEY `idx_users_created_at` (`created_at`),
  KEY `idx_users_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

### 2.2 配置管理模块

#### 2.2.1 系统配置表 (system_configs)
**表说明**: 存储系统全局配置

```sql
CREATE TABLE `system_configs` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '配置ID',
  `config_key` VARCHAR(100) NOT NULL COMMENT '配置键',
  `config_value` TEXT NOT NULL COMMENT '配置值(JSON格式)',
  `config_type` VARCHAR(20) NOT NULL COMMENT '配置类型: system, model, commercial',
  `description` VARCHAR(255) DEFAULT NULL COMMENT '配置描述',
  `is_encrypted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否加密: 0-否, 1-是',
  `is_public` TINYINT NOT NULL DEFAULT 0 COMMENT '是否公开: 0-否, 1-是(客户端可见)',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_configs_key` (`config_key`),
  KEY `idx_configs_type` (`config_type`),
  KEY `idx_configs_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置表';
```

**配置示例数据**:
```sql
-- 商用模式配置
INSERT INTO system_configs (config_key, config_value, config_type, description) VALUES
('commercial_mode', '{"enabled": false, "description": "商用模式开关"}', 'commercial', '商用模式配置');

-- 模型配置
INSERT INTO system_configs (config_key, config_value, config_type, description, is_encrypted) VALUES
('default_model', '{"llm": "claude-sonnet-4-5", "source": "Anthropic", "temperature": 0.7, "timeout_seconds": 600}', 'model', '默认模型配置', 0);

-- 系统配置
INSERT INTO system_configs (config_key, config_value, config_type, description) VALUES
('system_settings', '{"data_path": "./data", "use_tool_retriever": true, "max_concurrent_tasks": 100, "session_timeout": 30, "enable_registration": true}', 'system', '系统基础配置');
```

#### 2.2.2 配置变更日志表 (config_change_logs)
**表说明**: 记录配置变更历史

```sql
CREATE TABLE `config_change_logs` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `config_id` BIGINT NOT NULL COMMENT '配置ID',
  `config_key` VARCHAR(100) NOT NULL COMMENT '配置键',
  `old_value` TEXT DEFAULT NULL COMMENT '旧值',
  `new_value` TEXT NOT NULL COMMENT '新值',
  `operator_id` BIGINT NOT NULL COMMENT '操作人ID',
  `operator_type` VARCHAR(20) NOT NULL COMMENT '操作人类型: admin, system',
  `change_reason` VARCHAR(255) DEFAULT NULL COMMENT '变更原因',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_change_logs_config_id` (`config_id`),
  KEY `idx_change_logs_operator` (`operator_id`, `operator_type`),
  KEY `idx_change_logs_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='配置变更日志表';
```

### 2.3 配额管理模块

#### 2.3.1 配额模板表 (quota_templates)
**表说明**: 定义配额模板

```sql
CREATE TABLE `quota_templates` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '模板ID',
  `template_name` VARCHAR(100) NOT NULL COMMENT '模板名称',
  `description` VARCHAR(255) DEFAULT NULL COMMENT '模板描述',
  `daily_conversation_limit` INT NOT NULL DEFAULT 10 COMMENT '每日对话次数限制',
  `monthly_conversation_limit` INT NOT NULL DEFAULT 300 COMMENT '每月对话次数限制',
  `daily_token_limit` BIGINT NOT NULL DEFAULT 100000 COMMENT '每日Token限制',
  `monthly_token_limit` BIGINT NOT NULL DEFAULT 3000000 COMMENT '每月Token限制',
  `concurrent_task_limit` INT NOT NULL DEFAULT 3 COMMENT '并发任务限制',
  `storage_limit_mb` INT NOT NULL DEFAULT 1024 COMMENT '存储空间限制(MB)',
  `is_default` TINYINT NOT NULL DEFAULT 0 COMMENT '是否默认模板: 0-否, 1-是',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用, 1-启用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_quota_templates_name` (`template_name`),
  KEY `idx_quota_templates_default` (`is_default`),
  KEY `idx_quota_templates_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='配额模板表';
```

**示例数据**:
```sql
INSERT INTO quota_templates (template_name, description, daily_conversation_limit, monthly_conversation_limit, 
  daily_token_limit, monthly_token_limit, concurrent_task_limit, storage_limit_mb, is_default) VALUES
('免费版', '新用户默认配额', 10, 300, 100000, 3000000, 3, 1024, 1),
('基础版', '基础付费用户配额', 50, 1500, 500000, 15000000, 5, 5120, 0),
('专业版', '专业用户配额', 200, 6000, 2000000, 60000000, 10, 20480, 0),
('企业版', '企业用户配额', -1, -1, -1, -1, 50, 102400, 0);
```

#### 2.3.2 用户配额表 (user_quotas)
**表说明**: 存储用户的配额分配

```sql
CREATE TABLE `user_quotas` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '配额ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `template_id` BIGINT DEFAULT NULL COMMENT '配额模板ID',
  `daily_conversation_limit` INT NOT NULL COMMENT '每日对话次数限制',
  `monthly_conversation_limit` INT NOT NULL COMMENT '每月对话次数限制',
  `daily_token_limit` BIGINT NOT NULL COMMENT '每日Token限制',
  `monthly_token_limit` BIGINT NOT NULL COMMENT '每月Token限制',
  `concurrent_task_limit` INT NOT NULL COMMENT '并发任务限制',
  `storage_limit_mb` INT NOT NULL COMMENT '存储空间限制(MB)',
  `effective_from` DATETIME NOT NULL COMMENT '生效开始时间',
  `effective_until` DATETIME DEFAULT NULL COMMENT '生效结束时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_quotas_user` (`user_id`, `deleted_at`),
  KEY `idx_user_quotas_template` (`template_id`),
  KEY `idx_user_quotas_effective` (`effective_from`, `effective_until`),
  KEY `idx_user_quotas_deleted_at` (`deleted_at`),
  CONSTRAINT `fk_user_quotas_users` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_user_quotas_templates` FOREIGN KEY (`template_id`) REFERENCES `quota_templates` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户配额表';
```

#### 2.3.3 配额使用记录表 (quota_usage_records)
**表说明**: 记录用户配额使用情况

```sql
CREATE TABLE `quota_usage_records` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `record_date` DATE NOT NULL COMMENT '记录日期',
  `daily_conversation_used` INT NOT NULL DEFAULT 0 COMMENT '当日对话次数',
  `monthly_conversation_used` INT NOT NULL DEFAULT 0 COMMENT '当月对话次数',
  `daily_token_used` BIGINT NOT NULL DEFAULT 0 COMMENT '当日Token使用量',
  `monthly_token_used` BIGINT NOT NULL DEFAULT 0 COMMENT '当月Token使用量',
  `storage_used_mb` INT NOT NULL DEFAULT 0 COMMENT '存储使用量(MB)',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_quota_usage_user_date` (`user_id`, `record_date`),
  KEY `idx_quota_usage_date` (`record_date`),
  CONSTRAINT `fk_quota_usage_users` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='配额使用记录表';
```

### 2.4 对话管理模块

#### 2.4.1 对话表 (conversations)
**表说明**: 存储用户对话信息

```sql
CREATE TABLE `conversations` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '对话ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `title` VARCHAR(255) NOT NULL COMMENT '对话标题',
  `status` VARCHAR(20) NOT NULL DEFAULT 'active' COMMENT '状态: active-进行中, completed-已完成, failed-失败, cancelled-已取消',
  `model_name` VARCHAR(100) DEFAULT NULL COMMENT '使用的模型名称',
  `model_source` VARCHAR(50) DEFAULT NULL COMMENT '模型来源',
  `message_count` INT NOT NULL DEFAULT 0 COMMENT '消息数量',
  `total_tokens` BIGINT NOT NULL DEFAULT 0 COMMENT '总Token消耗',
  `total_duration_ms` BIGINT NOT NULL DEFAULT 0 COMMENT '总执行时长(毫秒)',
  `is_favorite` TINYINT NOT NULL DEFAULT 0 COMMENT '是否收藏: 0-否, 1-是',
  `is_pinned` TINYINT NOT NULL DEFAULT 0 COMMENT '是否置顶: 0-否, 1-是',
  `last_message_at` DATETIME DEFAULT NULL COMMENT '最后消息时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  KEY `idx_conversations_user_id` (`user_id`),
  KEY `idx_conversations_status` (`status`),
  KEY `idx_conversations_created_at` (`created_at`),
  KEY `idx_conversations_favorite` (`user_id`, `is_favorite`),
  KEY `idx_conversations_deleted_at` (`deleted_at`),
  CONSTRAINT `fk_conversations_users` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='对话表';
```

#### 2.4.2 消息表 (messages)
**表说明**: 存储对话中的消息

```sql
CREATE TABLE `messages` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  `conversation_id` BIGINT NOT NULL COMMENT '对话ID',
  `role` VARCHAR(20) NOT NULL COMMENT '角色: user-用户, assistant-助手, system-系统',
  `content` TEXT NOT NULL COMMENT '消息内容',
  `content_type` VARCHAR(20) NOT NULL DEFAULT 'text' COMMENT '内容类型: text, markdown, code, image',
  `tokens` INT NOT NULL DEFAULT 0 COMMENT 'Token消耗',
  `parent_message_id` BIGINT DEFAULT NULL COMMENT '父消息ID(用于消息树)',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_messages_conversation` (`conversation_id`),
  KEY `idx_messages_parent` (`parent_message_id`),
  KEY `idx_messages_created_at` (`created_at`),
  CONSTRAINT `fk_messages_conversations` FOREIGN KEY (`conversation_id`) REFERENCES `conversations` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='消息表';
```

#### 2.4.3 执行步骤表 (execution_steps)
**表说明**: 记录 Agent 执行的详细步骤

```sql
CREATE TABLE `execution_steps` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '步骤ID',
  `conversation_id` BIGINT NOT NULL COMMENT '对话ID',
  `message_id` BIGINT NOT NULL COMMENT '消息ID',
  `step_order` INT NOT NULL COMMENT '步骤顺序',
  `step_type` VARCHAR(50) NOT NULL COMMENT '步骤类型: reasoning, tool_call, result',
  `step_name` VARCHAR(100) DEFAULT NULL COMMENT '步骤名称',
  `tool_name` VARCHAR(100) DEFAULT NULL COMMENT '工具名称',
  `tool_input` TEXT DEFAULT NULL COMMENT '工具输入(JSON)',
  `tool_output` TEXT DEFAULT NULL COMMENT '工具输出',
  `status` VARCHAR(20) NOT NULL DEFAULT 'running' COMMENT '状态: running, success, failed',
  `error_message` TEXT DEFAULT NULL COMMENT '错误信息',
  `duration_ms` INT NOT NULL DEFAULT 0 COMMENT '执行时长(毫秒)',
  `started_at` DATETIME NOT NULL COMMENT '开始时间',
  `completed_at` DATETIME DEFAULT NULL COMMENT '完成时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_steps_conversation` (`conversation_id`),
  KEY `idx_steps_message` (`message_id`),
  KEY `idx_steps_order` (`conversation_id`, `step_order`),
  KEY `idx_steps_status` (`status`),
  CONSTRAINT `fk_steps_conversations` FOREIGN KEY (`conversation_id`) REFERENCES `conversations` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_steps_messages` FOREIGN KEY (`message_id`) REFERENCES `messages` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='执行步骤表';
```

### 2.5 Know-How 文档管理模块

#### 2.5.1 Know-How 文档表 (knowhow_documents)
**表说明**: 存储 Know-How 文档

```sql
CREATE TABLE `knowhow_documents` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '文档ID',
  `doc_id` VARCHAR(100) NOT NULL COMMENT '文档唯一标识(文件名)',
  `title` VARCHAR(255) NOT NULL COMMENT '文档标题',
  `short_description` VARCHAR(500) DEFAULT NULL COMMENT '简短描述',
  `content` LONGTEXT NOT NULL COMMENT '文档内容(Markdown)',
  `content_without_metadata` LONGTEXT DEFAULT NULL COMMENT '去除元数据的内容',
  `category` VARCHAR(100) DEFAULT NULL COMMENT '分类',
  `tags` VARCHAR(500) DEFAULT NULL COMMENT '标签(逗号分隔)',
  `authors` VARCHAR(500) DEFAULT NULL COMMENT '作者',
  `affiliations` VARCHAR(500) DEFAULT NULL COMMENT '机构',
  `version` VARCHAR(50) DEFAULT NULL COMMENT '版本',
  `last_updated` DATE DEFAULT NULL COMMENT '最后更新日期',
  `license` VARCHAR(100) DEFAULT NULL COMMENT '许可证',
  `commercial_use` TINYINT NOT NULL DEFAULT 0 COMMENT '商用许可: 0-不允许, 1-允许',
  `source_url` VARCHAR(500) DEFAULT NULL COMMENT '来源链接',
  `citation` TEXT DEFAULT NULL COMMENT '引用信息',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用, 1-启用, 2-草稿',
  `priority` INT NOT NULL DEFAULT 0 COMMENT '优先级(用于检索排序)',
  `view_count` INT NOT NULL DEFAULT 0 COMMENT '查看次数',
  `usage_count` INT NOT NULL DEFAULT 0 COMMENT '使用次数(被Agent调用)',
  `file_path` VARCHAR(500) DEFAULT NULL COMMENT '文件路径',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_documents_doc_id` (`doc_id`),
  KEY `idx_documents_category` (`category`),
  KEY `idx_documents_status` (`status`),
  KEY `idx_documents_commercial` (`commercial_use`),
  KEY `idx_documents_priority` (`priority`),
  KEY `idx_documents_deleted_at` (`deleted_at`),
  FULLTEXT KEY `ft_documents_content` (`title`, `short_description`, `content`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='Know-How文档表';
```

**说明**:
- `category`: 单个分类字符串，如 "数据分析"、"实验技术"
- `tags`: 多个标签用逗号分隔，如 "single-cell,annotation,RNA-seq"
- 简化设计，不使用关联表
#### 2.5.2 文档版本历史表 (knowhow_document_versions)
**表说明**: 记录文档的版本历史

```sql
CREATE TABLE `knowhow_document_versions` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '版本ID',
  `document_id` BIGINT NOT NULL COMMENT '文档ID',
  `version_number` INT NOT NULL COMMENT '版本号',
  `content` LONGTEXT NOT NULL COMMENT '文档内容快照',
  `change_summary` VARCHAR(500) DEFAULT NULL COMMENT '变更摘要',
  `editor_id` BIGINT NOT NULL COMMENT '编辑者ID',
  `editor_type` VARCHAR(20) NOT NULL COMMENT '编辑者类型: admin, system',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_versions_document` (`document_id`, `version_number`),
  KEY `idx_versions_editor` (`editor_id`, `editor_type`),
  CONSTRAINT `fk_versions_documents` FOREIGN KEY (`document_id`) REFERENCES `knowhow_documents` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='文档版本历史表';
```

### 2.6 日志审计模块

#### 2.6.1 操作日志表 (operation_logs)
**表说明**: 记录用户和管理员的操作日志

```sql
CREATE TABLE `operation_logs` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `operator_id` BIGINT NOT NULL COMMENT '操作人ID',
  `operator_type` VARCHAR(20) NOT NULL COMMENT '操作人类型: admin, user, system',
  `operator_name` VARCHAR(100) DEFAULT NULL COMMENT '操作人名称',
  `operation_type` VARCHAR(50) NOT NULL COMMENT '操作类型: create, update, delete, login, logout',
  `operation_module` VARCHAR(50) NOT NULL COMMENT '操作模块: user, config, conversation, knowhow',
  `operation_desc` VARCHAR(255) NOT NULL COMMENT '操作描述',
  `request_method` VARCHAR(10) DEFAULT NULL COMMENT '请求方法: GET, POST, PUT, DELETE',
  `request_url` VARCHAR(500) DEFAULT NULL COMMENT '请求URL',
  `request_params` TEXT DEFAULT NULL COMMENT '请求参数(JSON)',
  `request_ip` VARCHAR(50) DEFAULT NULL COMMENT '请求IP',
  `user_agent` VARCHAR(500) DEFAULT NULL COMMENT 'User Agent',
  `response_status` INT DEFAULT NULL COMMENT '响应状态码',
  `response_time_ms` INT DEFAULT NULL COMMENT '响应时间(毫秒)',
  `error_message` TEXT DEFAULT NULL COMMENT '错误信息',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_logs_operator` (`operator_id`, `operator_type`),
  KEY `idx_logs_type` (`operation_type`),
  KEY `idx_logs_module` (`operation_module`),
  KEY `idx_logs_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='操作日志表';
```

#### 2.6.2 登录日志表 (login_logs)
**表说明**: 记录登录日志

```sql
CREATE TABLE `login_logs` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `user_id` BIGINT DEFAULT NULL COMMENT '用户ID',
  `user_type` VARCHAR(20) NOT NULL COMMENT '用户类型: admin, user',
  `username` VARCHAR(100) NOT NULL COMMENT '用户名',
  `login_type` VARCHAR(20) NOT NULL COMMENT '登录方式: password, code, oauth',
  `login_status` VARCHAR(20) NOT NULL COMMENT '登录状态: success, failed',
  `fail_reason` VARCHAR(255) DEFAULT NULL COMMENT '失败原因',
  `ip_address` VARCHAR(50) NOT NULL COMMENT 'IP地址',
  `location` VARCHAR(100) DEFAULT NULL COMMENT '地理位置',
  `user_agent` VARCHAR(500) DEFAULT NULL COMMENT 'User Agent',
  `device_type` VARCHAR(50) DEFAULT NULL COMMENT '设备类型: PC, Mobile, Tablet',
  `browser` VARCHAR(50) DEFAULT NULL COMMENT '浏览器',
  `os` VARCHAR(50) DEFAULT NULL COMMENT '操作系统',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_login_logs_user` (`user_id`, `user_type`),
  KEY `idx_login_logs_status` (`login_status`),
  KEY `idx_login_logs_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='登录日志表';
```

---

## 3. 索引优化建议

### 3.1 高频查询索引
- 用户登录: `users.username`, `users.email`, `users.phone`
- 对话列表: `conversations.user_id`, `conversations.created_at`
- 配额查询: `user_quotas.user_id`, `quota_usage_records.user_id`
- 文档搜索: 全文索引 `knowhow_documents(title, content)`

### 3.2 复合索引
- 对话筛选: `(user_id, status, created_at)`
- 配额使用: `(user_id, record_date)`
- 执行步骤: `(conversation_id, step_order)`

### 3.3 分区建议
- `operation_logs`: 按月分区 (RANGE PARTITION by `created_at`)
- `login_logs`: 按月分区
- `system_monitor_records`: 按月分区
- `quota_usage_records`: 按月分区

---

## 4. 数据迁移与初始化

### 4.1 初始化脚本

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS biomni_app 
  DEFAULT CHARACTER SET utf8mb4 
  COLLATE utf8mb4_unicode_ci;

USE biomni_app;

-- 执行所有表创建语句 (见上文)

-- 创建默认管理员 (密码在应用启动时随机生成)
-- INSERT INTO admins (username, password, email, real_name, role, status, force_password_change) VALUES
-- ('admin', '$2a$10$...', 'admin@biomni.com', '系统管理员', 'super_admin', 1, 1);

-- 创建默认配额模板
INSERT INTO quota_templates (template_name, description, daily_conversation_limit, 
  monthly_conversation_limit, daily_token_limit, monthly_token_limit, 
  concurrent_task_limit, storage_limit_mb, is_default) VALUES
('免费版', '新用户默认配额', 10, 300, 100000, 3000000, 3, 1024, 1);

-- 创建默认系统配置
INSERT INTO system_configs (config_key, config_value, config_type, description) VALUES
('commercial_mode', '{"enabled": false}', 'commercial', '商用模式配置'),
('default_model', '{"llm": "claude-sonnet-4-5", "source": "Anthropic", "temperature": 0.7}', 'model', '默认模型配置'),
('system_settings', '{"max_concurrent_tasks": 100, "session_timeout": 30}', 'system', '系统配置');

-- 创建默认文档分类
-- 不再需要单独的分类表，分类直接存储在文档表的 category 字段中
```

### 4.2 Know-How 文档导入

从 `agent/biomni/know_how/` 目录导入现有文档:

```python
# Python 脚本示例
from biomni.know_how import KnowHowLoader
import mysql.connector

loader = KnowHowLoader()
documents = loader.get_all_documents()

conn = mysql.connector.connect(...)
cursor = conn.cursor()

for doc in documents:
    cursor.execute("""
        INSERT INTO knowhow_documents 
        (doc_id, title, short_description, content, content_without_metadata,
         category, tags, authors, license, commercial_use, status)
        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
    """, (
        doc['id'],
        doc['name'],
        doc['description'],
        doc['content'],
        doc.get('content_without_metadata'),
        '数据分析',  # 默认分类
        'single-cell,annotation',  # 示例标签
        doc['metadata'].get('authors'),
        doc['metadata'].get('license'),
        1 if doc['metadata'].get('commercial_use') == '✅ Allowed' else 0,
        1
    ))

conn.commit()
```

---

## 5. 性能优化建议

### 5.1 查询优化
- 使用连接池 (HikariCP)
- 慢查询日志监控
- 定期 EXPLAIN 分析
- 避免 SELECT *
- 合理使用分页

### 5.2 缓存策略
- Redis 缓存热点数据:
  - 用户信息 (TTL: 30分钟)
  - 系统配置 (TTL: 1小时)
  - 文档列表 (TTL: 10分钟)
  - 配额信息 (TTL: 5分钟)

### 5.3 读写分离
- 主库: 写操作
- 从库: 读操作 (对话列表、文档查询、监控数据)

### 5.4 数据归档
- 定期归档历史数据:
  - 6个月前的对话记录
  - 3个月前的操作日志
  - 1个月前的监控数据

---

## 6. 备份与恢复

### 6.1 备份策略
- **全量备份**: 每天凌晨 2:00
- **增量备份**: 每 6 小时
- **binlog 备份**: 实时
- **保留周期**: 30 天

### 6.2 恢复测试
- 每月进行恢复演练
- 验证数据完整性
- 测试恢复时间

---

## 7. 安全建议

### 7.1 数据安全
- 敏感字段加密 (AES-256):
  - 密码 (BCrypt)
  - API Key
  - 自定义模型配置
- SQL 注入防护
- 参数化查询

### 7.2 访问控制
- 最小权限原则
- 应用账号与管理账号分离
- 定期审计权限

### 7.3 数据脱敏
- 日志中脱敏敏感信息
- 导出数据脱敏处理

---

## 8. 监控指标

### 8.1 数据库监控
- 连接数
- QPS/TPS
- 慢查询数量
- 锁等待
- 表空间使用率

### 8.2 业务监控
- 用户注册量
- 对话创建量
- Token 消耗量
- 配额使用率
- 文档访问量

---

## 附录 A: ER 图

```
[users] 1---N [conversations] 1---N [messages]
   |                |                   |
   |                |                   |
   1                1                   1
   |                |                   |
   N                N                   N
[user_quotas]  [execution_steps]  [conversation_shares]
   |
   |
   N
   |
   1
[quota_templates]

[knowhow_documents] N---N [knowhow_tags]
        |                      |
        |                      |
        N                      |
        |                      |
        1                      |
[knowhow_categories]           |
        |                      |
        |                      |
        N----------------------N
[knowhow_document_tags]
```

## 附录 B: 表关系说明

### 核心关系
1. **用户 → 对话**: 一对多 (一个用户可以创建多个对话)
2. **对话 → 消息**: 一对多 (一个对话包含多条消息)
3. **对话 → 执行步骤**: 一对多 (一个对话有多个执行步骤)
4. **用户 → 配额**: 一对一 (每个用户有一个配额配置)
5. **文档 → 标签**: 多对多 (文档可以有多个标签)
6. **文档 → 分类**: 多对一 (文档属于一个分类)

---

**文档版本**: v1.0  
**最后更新**: 2025-01-18  
**维护者**: Biomni Team
