# 简化配额方案（独立表，只限制总 Token）

## 1. 核心设计

### 1.1 只做一件事
- ✅ 每个用户有总 Token 配额
- ✅ 累计使用，不重置
- ✅ 用完即止

### 1.2 不做的事
- ❌ 不限制每日对话次数
- ❌ 不限制每日 Token
- ❌ 不限制并发任务
- ❌ 不需要 Redis

---

## 2. 数据库设计

### 2.1 创建独立的配额表

```sql
CREATE TABLE `user_quotas` (
  `id` INT NOT NULL AUTO_INCREMENT COMMENT '配额ID',
  `user_id` INT NOT NULL COMMENT '用户ID',
  `total_token_limit` INT NOT NULL DEFAULT 1000000 COMMENT '总Token配额',
  `total_token_used` INT NOT NULL DEFAULT 0 COMMENT '已使用Token数',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_quotas_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户配额表';
```

**优势**：
- ✅ 独立表，不影响 `admin` 表的缓存
- ✅ 可以单独更新配额，不触发用户缓存失效
- ✅ 便于扩展（后续可以添加其他配额字段）

### 2.2 初始化数据

```sql
-- 为所有现有用户创建默认配额
INSERT INTO user_quotas (user_id, total_token_limit, total_token_used)
SELECT id, 1000000, 0 FROM admin;
```

---

## 3. Python 实现

### 3.1 数据模型

```python
# models/models.py
class UserQuota(Base):
    __tablename__ = 'user_quotas'
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(Integer, nullable=False, unique=True)
    total_token_limit = Column(Integer, nullable=False, default=1000000)
    total_token_used = Column(Integer, nullable=False, default=0)
    created_at = Column(DateTime, nullable=False, default=datetime.now)
    updated_at = Column(DateTime, nullable=False, default=datetime.now, onupdate=datetime.now)
```

### 3.2 配额检查

```python
@router.websocket("/chat/{conversation_id}")
async def websocket_endpoint(...):
    data = await websocket.receive_json()
    
    if data['type'] == 'send_message':
        # 检查配额
        from models.models import UserQuota
        
        quota = db.query(UserQuota).filter(UserQuota.user_id == user_id).first()
        
        if not quota:
            # 如果没有配额记录，创建默认配额
            quota = UserQuota(user_id=user_id, total_token_limit=1000000, total_token_used=0)
            db.add(quota)
            db.commit()
        
        if quota.total_token_used >= quota.total_token_limit:
            await websocket.send_json({
                'type': 'quota_exceeded',
                'error': f'Token 配额已用完（{quota.total_token_used}/{quota.total_token_limit}）',
                'quota': {
                    'totalTokenLimit': quota.total_token_limit,
                    'totalTokenUsed': quota.total_token_used,
                    'remaining': 0
                }
            })
            return
        
        # 执行 Agent
        await handle_agent_execution(...)
```

### 3.3 配额更新

```python
def get_result(self):
    # 保存 AI 消息
    assistant_message = Message(...)
    self.db.add(assistant_message)
    self.db.commit()
    
    # 更新用户配额（独立表）
    from models.models import UserQuota
    
    quota = self.db.query(UserQuota).filter(UserQuota.user_id == self.user_id).first()
    if quota:
        quota.total_token_used += (self.total_input_tokens + self.total_output_tokens)
        quota.updated_at = datetime.now()
        self.db.commit()
    
    # 更新对话统计
    conversation = self.db.query(Conversation).get(self.conversation_id)
    if conversation:
        conversation.message_count += 1
        conversation.total_tokens += assistant_message.tokens
        conversation.total_duration_ms += self.total_duration_ms
        conversation.last_message_at = datetime.now()
        conversation.updated_at = datetime.now()
        self.db.commit()
    
    return {...}
```

---

## 4. 优势对比

### 独立表 vs 在 admin 表中

| 维度 | 独立表 | admin 表 |
|------|--------|----------|
| 缓存影响 | ✅ 不影响用户缓存 | ❌ 更新配额会失效缓存 |
| 扩展性 | ✅ 易于添加字段 | ❌ admin 表会越来越大 |
| 查询性能 | ✅ 独立索引 | ❌ 需要查询 admin 表 |
| 数据隔离 | ✅ 配额数据独立 | ❌ 混在一起 |
| 实现复杂度 | ⚠️ 稍复杂 | ✅ 简单 |

**结论**：独立表更好！

---

## 5. 总结

### 数据库
- ✅ 创建 `user_quotas` 表（4 个字段）
- ✅ 不影响 `admin` 表
- ✅ 避免缓存问题

### Python
- ✅ 查询 `user_quotas` 表
- ✅ 更新 `user_quotas` 表
- ✅ 不触及 `admin` 表

### 优势
- ✅ 数据隔离
- ✅ 缓存友好
- ✅ 易于扩展

---

**文档版本**: v3.0（独立表版）  
**创建时间**: 2025-01-20  
**维护者**: Biomni Team  
**状态**: 方案设计，待实现


---

## 3. Python 实现

### 3.1 配额检查（WebSocket 接收消息时）

```python
@router.websocket("/chat/{conversation_id}")
async def websocket_endpoint(...):
    # 接收消息
    data = await websocket.receive_json()
    
    if data['type'] == 'send_message':
        # 1. 检查配额
        from models.models import Admin
        
        user = db.query(Admin).get(user_id)
        if user.total_token_used >= user.total_token_limit:
            await websocket.send_json({
                'type': 'quota_exceeded',
                'error': f'Token 配额已用完（{user.total_token_used}/{user.total_token_limit}）'
            })
            return
        
        # 2. 执行 Agent
        await handle_agent_execution(...)
```

### 3.2 Token 统计（Callback）

```python
class WebSocketCallback:
    def __init__(self, conversation_id, manager, db, user_id):
        self.conversation_id = conversation_id
        self.manager = manager
        self.db = db
        self.user_id = user_id  # 新增：保存 user_id
        self.total_input_tokens = 0
        self.total_output_tokens = 0
    
    def on_llm_call(self, input_tokens, output_tokens):
        """LLM 调用时统计 Token"""
        self.total_input_tokens += input_tokens
        self.total_output_tokens += output_tokens
        
        # 实时推送到前端
        await self.manager.send_message(str(self.conversation_id), {
            'type': 'token_usage',
            'inputTokens': input_tokens,
            'outputTokens': output_tokens,
            'totalTokens': self.total_input_tokens + self.total_output_tokens
        })
```

### 3.3 配额更新（执行完成后）

```python
def get_result(self):
    # 保存 AI 消息
    assistant_message = Message(...)
    self.db.add(assistant_message)
    self.db.commit()
    
    # 更新用户的 Token 使用量
    from models.models import Admin
    
    user = self.db.query(Admin).get(self.user_id)
    if user:
        user.total_token_used += (self.total_input_tokens + self.total_output_tokens)
        self.db.commit()
    
    # 更新对话统计
    conversation = self.db.query(Conversation).get(self.conversation_id)
    if conversation:
        conversation.message_count += 1
        conversation.total_tokens += assistant_message.tokens
        conversation.total_duration_ms += self.total_duration_ms
        conversation.last_message_at = datetime.now()
        conversation.updated_at = datetime.now()
        self.db.commit()
    
    return {...}
```

---

## 4. 前端展示

### 4.1 获取配额信息

```typescript
// API 调用
async getQuota(): Promise<QuotaInfo> {
  const response = await apiClient.get('/api/auth/me');
  const user = response.data.data;
  
  return {
    totalTokenLimit: user.totalTokenLimit,
    totalTokenUsed: user.totalTokenUsed,
    remaining: user.totalTokenLimit - user.totalTokenUsed
  };
}
```

### 4.2 显示配额

```typescript
// 底部显示
<Tag color="blue">
  Token: {quota.totalTokenUsed.toLocaleString()} / {quota.totalTokenLimit.toLocaleString()}
</Tag>

// 或者显示剩余
<Tag color={quota.remaining > 10000 ? 'green' : 'red'}>
  剩余 Token: {quota.remaining.toLocaleString()}
</Tag>
```

### 4.3 配额不足提示

```typescript
// WebSocket 接收
if (data.type === 'quota_exceeded') {
  message.error('Token 配额已用完，请联系管理员');
  setIsExecuting(false);
}
```

---

## 5. 管理后台配置

### 5.1 用户管理页面

```
用户列表增加列：
- Token 配额：1,000,000
- 已使用：250,000
- 剩余：750,000
- 使用率：25%
```

### 5.2 编辑用户配额

```
编辑用户时可以设置：
- 总 Token 配额：输入框
- 重置使用量：按钮（将 total_token_used 设为 0）
```

---

## 6. 实现步骤

### 6.1 数据库（5 分钟）

```sql
ALTER TABLE `admin` 
ADD COLUMN `total_token_limit` INT NOT NULL DEFAULT 1000000 COMMENT '总Token配额',
ADD COLUMN `total_token_used` INT NOT NULL DEFAULT 0 COMMENT '已使用Token数';
```

### 6.2 Python 代码（30 分钟）

1. 修改 `Admin` 模型（添加字段）
2. 修改 `WebSocketCallback`（添加 `user_id`，统计 Token）
3. 修改 `websocket.py`（检查配额）
4. 修改 `callback.get_result()`（更新配额）

### 6.3 前端代码（15 分钟）

1. 修改 `User` 类型（添加字段）
2. 修改底部显示（显示配额）
3. 处理配额不足消息

### 6.4 管理后台（可选）

1. 用户列表显示配额
2. 编辑用户配额

---

## 7. 优势

### 简单
- ✅ 只需 2 个字段
- ✅ 不需要额外的表
- ✅ 不需要 Redis
- ✅ 不需要定时任务

### 准确
- ✅ Token 统计准确（从 LLM 获取）
- ✅ 实时更新
- ✅ 数据持久化

### 灵活
- ✅ 管理员可以随时调整配额
- ✅ 可以重置使用量
- ✅ 后续可以扩展（每日限制等）

---

## 8. 示例

### 8.1 用户配额

```
用户 A：
- 总配额：1,000,000 Token
- 已使用：250,000 Token
- 剩余：750,000 Token
- 状态：正常

用户 B：
- 总配额：1,000,000 Token
- 已使用：999,500 Token
- 剩余：500 Token
- 状态：即将用完
```

### 8.2 配额用完

```
用户发送消息 → Python 检查配额 → 配额不足 → 返回错误
前端显示：Token 配额已用完，请联系管理员
```

---

## 9. 总结

### 核心流程

```
1. 用户发送消息
   ↓
2. Python 检查 total_token_used < total_token_limit
   ↓
3. 执行 Agent，统计 Token
   ↓
4. 更新 total_token_used += tokens
   ↓
5. 前端显示剩余配额
```

### 实现工作量

- 数据库：5 分钟
- Python：30 分钟
- 前端：15 分钟
- **总计：50 分钟**

简单、准确、易维护！

---

**文档版本**: v2.0（简化版）  
**创建时间**: 2025-01-20  
**维护者**: Biomni Team  
**状态**: 方案设计，待实现
